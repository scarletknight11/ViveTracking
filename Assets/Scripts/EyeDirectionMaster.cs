using UnityEngine;
using Photon.Pun.Demo.PunBasics;


public class EyeDirectionMaster : MonoBehaviour
{
    public Handle3DEyes.Orientation currentEyeDirection;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
